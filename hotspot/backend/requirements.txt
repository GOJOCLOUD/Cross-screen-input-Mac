# Python依赖包

# FastAPI框架
fastapi
# 不用 uvicorn[standard]：watchfiles/uvloop 等含单架构原生扩展，
# macOS 上 PyInstaller --target-architecture universal2 会报 IncompatibleBinaryArchError。
# 运行时未开 --reload，无需 watchfiles；性能略低于 httptools/uvloop，可接受。
uvicorn

# 剪贴板操作
pyperclip

# 键盘快捷键执行
pynput>=1.7.6

# 跨域支持
fastapi-cors

# 图像处理（截图功能）
Pillow>=9.0.0

# 系统进程管理
psutil>=5.8.0

# 文件上传支持
python-multipart

# 配置管理（Pydantic V2需要）
pydantic-settings

# 类型提示扩展
typing_extensions

# macOS 专用（仅 Darwin 安装）
pyobjc-core>=9.0; sys_platform == 'darwin'
pyobjc-framework-Quartz>=9.0; sys_platform == 'darwin'

# WebSocket 客户端（局域网中转站对接）
websockets>=12.0
